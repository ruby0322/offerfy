apiVersion: batch/v1
kind: CronJob
metadata:
  name: jobs-ingest
spec:
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: ingest
              imagePullPolicy: IfNotPresent
